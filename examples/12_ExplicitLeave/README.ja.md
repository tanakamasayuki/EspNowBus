# 12_ExplicitLeave

離脱、再参加、再起動を対話的に試すサンプルです。

## このサンプルで確認できること

- Wi-Fi を止める / 止めない `end(...)`
- 離脱後の再 `begin()`
- bus 再開後のブロードキャスト送信
- 状態変化中の join / send result コールバック

## 使い方

同じスケッチを 2 台に書き込み、シリアルモニタから `e`、`p`、`b`、`s`、`r` などのコマンドを送って確認します。
