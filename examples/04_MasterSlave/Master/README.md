# 04_MasterSlave / Master

Master-side sketch for the master/slave example pair.

## What It Does

- Uses a fixed group name shared with `Slave`
- Accepts peer registrations
- Prints inbound payloads from slave nodes

Flash this sketch to the board that will act as the receiver side.
