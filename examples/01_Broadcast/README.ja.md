# 01_Broadcast

`EspNowBus` の最小ブロードキャストサンプルです。

## このサンプルで確認できること

- `EspNowBus::begin()` の基本初期化
- `bus.broadcast(...)` による定期送信
- 受信コールバックの基本動作

## 使い方

1. 同じスケッチを 2 台以上の ESP32 に書き込みます。
2. それぞれのシリアルモニタを開きます。
3. 定期的に `hello broadcast` が表示されることを確認します。

同じ `groupName` を使うノード同士でブロードキャストを受信できます。
