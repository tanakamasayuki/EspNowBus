# 11_FullConfigTemplate

Template sketch that sets every `EspNowBus::Config` field explicitly.

## What It Shows

- All current config fields
- The default values used by the library
- A good starting point when building your own sketch

## How to Use

Use this sketch as a copy-and-edit template when you want to customize settings but keep the full config visible.
