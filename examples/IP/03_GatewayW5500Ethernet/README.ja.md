# 03_GatewayW5500Ethernet

W5500 Ethernet を uplink に使う gateway 側 `EspNowIP` サンプル予定です。

## このサンプルで確認できること

- gateway 側 uplink としての Ethernet 利用
- 有線 uplink でも同じ `routing + IPv4 NAT` モデルを使う構成
- Wi-Fi STA uplink より実運用に寄せた gateway 例

## 想定用途

有線 Ethernet uplink を使える環境で、より実運用向きの gateway サンプルとして使います。

