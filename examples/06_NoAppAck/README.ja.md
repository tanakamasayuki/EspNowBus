# 06_NoAppAck

論理 ACK を無効にした状態を確認するサンプルです。

## このサンプルで確認できること

- `enableAppAck = false`
- 論理 ACK なしでの送信ステータス
- このモードでは `onAppAck(...)` が基本的に呼ばれないこと

## 使い方

同じスケッチを 2 台に書き込み、`05_SendStatusDemo` とログを比較してください。
